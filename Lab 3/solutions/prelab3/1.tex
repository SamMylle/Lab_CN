In the interface config mode (config-if) reached by using the command interface \emph{<interface> <...>}, \emph{mtu <mtu\_size>} configures the size of the maximum transmission unit for that interface.