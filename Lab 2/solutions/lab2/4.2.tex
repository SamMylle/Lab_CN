eth0 sends packets on the network so there's no guarantee that he receives as many packets as he sends.
The lo interface sends packets to himself so he receives the same amount of packets as he sends.